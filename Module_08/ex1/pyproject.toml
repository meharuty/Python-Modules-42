[tool.poetry]
name = "loading"
version = "0.1.0"
description = "Matrix loading exercise"
authors = ["Meline"]

[tool.poetry.dependencies]
python = "^3.11"
numpy = "*"
pandas = "*"
matplotlib = "*"
requests = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
